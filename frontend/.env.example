# Supabase (public values — safe to expose in the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend API
VITE_API_BASE_URL=http://localhost:5000

# Set 'false' to switch from mock mode to live Supabase + Flask
VITE_USE_MOCK=true
